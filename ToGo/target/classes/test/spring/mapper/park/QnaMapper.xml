<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-Mapper.dtd">
<mapper namespace="test.spring.mapper.park.QnaMapper">
<<<<<<< HEAD
	<insert id="insert">
		INSERT INTO qna_board (num, root, title, content, writer, filename, filepath)
		VALUES (qna_board_seq.NEXTVAL, qna_board_seq.CURRVAL, #{title }, #{content }, #{writer }, #{filename, jdbcType=VARCHAR }, #{filepath, jdbcType=VARCHAR })
=======
	<insert id="qnaInsert">
		INSERT INTO qna_board (num, root, title, content, writer)
		VALUES (qna_board_seq.NEXTVAL, qna_board_seq.CURRVAL, #{title }, #{content }, #{writer })
>>>>>>> develop_Park
	</insert>
	
	<select id="qnaDetail" resultType="test.spring.component.park.QnaDTO">
		SELECT *
		FROM qna_board 
		WHERE num=#{num}

	</select>
	
<<<<<<< HEAD
	<update id="read">
		UPDATE qna_board SET readcnt = readcnt + 1 WHERE num=#{num}	
	</update>
	
	<delete id="delete">
		DELETE FROM qna_board WHERE num=#{num }
	</delete>
	
	<update id="update">
		UPDATE qna_board SET title = #{title }, content = #{content }, filename = #{filename, jdbcType=VARCHAR }, filepath = #{filepath, jdbcType=VARCHAR }
		WHERE num = #{num}
	</update>
	
	<select id="list" resultType="test.spring.component.park.QnaDTO">
		SELECT *
		FROM (SELECT ROWNUM no, q.*, (SELECT name FROM member m WHERE m.id = writer) name
			FROM (SELECT * FROM qna_board <include refid="search_where" /> ORDER BY root, step DESC) q
			ORDER BY num DESC) q
		WHERE num BETWEEN #{beginList } and #{endList }
	</select>
	
	<select id="totalList" resultType="integer">
		SELECT COUNT(*) FROM qna_board <include refid="search_where"/>
	</select>
	
	<insert id="reply_insert">
=======
	<update id="qnaRead">
		UPDATE qna_board SET readcnt = readcnt + 1 WHERE num=#{num}	
	</update>
	
	<delete id="qnaDelete">
		DELETE FROM qna_board WHERE num=#{num }
	</delete>
	
	<update id="qnaUpdate">
		UPDATE qna_board SET title = #{title }, content = #{content }
		WHERE num = #{num}
	</update>
	
<!-- 	<select id="list" resultType="test.spring.component.park.QnaDTO"> -->
<!-- 		SELECT * -->
<!-- 		FROM (SELECT ROWNUM no, q.*, (SELECT name FROM member m WHERE m.id = writer) name -->
<!-- 			FROM (SELECT * FROM qna_board <include refid="search_where" /> ORDER BY root, step DESC) q -->
<!-- 			ORDER BY num DESC) q -->
<!-- 		WHERE num BETWEEN #{beginList } and #{endList } -->
<!-- 	</select> -->
	<!-- 게시물 목록 조회 -->
	<select id="list"
	    parameterType="test.spring.component.park.QnaDTO"
	    resultType="test.spring.component.park.QnaDTO">
	    SELECT *
	    FROM (
	        SELECT a.*
	        FROM qna_board a
	        <if test='option == "all"'>
		       where (title LIKE '%' || #{keyword, jdbcType=VARCHAR} || '%'
		        OR content LIKE '%' || #{keyword, jdbcType=VARCHAR} || '%'
		        OR writer LIKE '%' || #{keyword, jdbcType=VARCHAR} || '%')
		    </if>
		    <if test='option == "title"'>
		       where title LIKE '%' || #{keyword, jdbcType=VARCHAR} || '%'
		    </if>
		    <if test="option == 'content'">
		       where content LIKE '%' || #{keyword, jdbcType=VARCHAR} || '%'
		    </if>
		    <if test="option == 'id'">
		       where writer LIKE '%' || #{keyword, jdbcType=VARCHAR} || '%'
		    </if>
		    )
	    WHERE num BETWEEN #{beginList} AND #{endList}
	    ORDER BY writedate DESC, num DESC
	</select>
	
	<select id="totalList" resultType="integer">
		SELECT COUNT(*) FROM qna_board  
			<if test='option == "all"'>
		       where (title LIKE '%' || #{keyword, jdbcType=VARCHAR} || '%'
		        OR content LIKE '%' || #{keyword, jdbcType=VARCHAR} || '%'
		        OR writer LIKE '%' || #{keyword, jdbcType=VARCHAR} || '%')
		    </if>
		    <if test='option == "title"'>
		       where title LIKE '%' || #{keyword, jdbcType=VARCHAR} || '%'
		    </if>
		    <if test="option == 'content'">
		       where content LIKE '%' || #{keyword, jdbcType=VARCHAR} || '%'
		    </if>
		    <if test="option == 'id'">
		       where writer LIKE '%' || #{keyword, jdbcType=VARCHAR} || '%'
		    </if>
	</select>
	
	<insert id="qnaReplyInsert">
>>>>>>> develop_Park
		<!-- 원글의 step보다 더 큰 step을 가진 글이 있다면 그 글들의 step을 먼저 +1 한다. -->
		<![CDATA[
			{CALL DECLARE BEGIN
				UPDATE qna_board SET step = step + 1
				WHERE root = #{root } AND step > #{step };
				
<<<<<<< HEAD
				INSERT INTO qna_board (num, root, title, content, writer, step, indent, filename, filepath)
				VALUES (qna_board_seq.NEXTVAL, #{root }, #{title }, #{content }, #{writer }, #{step } + 1, #{indent } + 1, #{filename, jdbcType=VARCHAR }, #{filepath, jdbcType=VARCHAR });
=======
				INSERT INTO qna_board (num, root, title, content, writer, step, indent)
				VALUES (qna_board_seq.NEXTVAL, #{root }, #{title }, #{content }, #{writer }, #{step } + 1, #{indent } + 1);
>>>>>>> develop_Park
			END}
		]]>
	</insert>
	
<<<<<<< HEAD
	<sql id="search_where">
		<if test="search == 'title' or search == 'content'">
			WHERE ${search } LIKE '%' || #{keyword } || '%'
		</if>
		<if test="search == 'writer'">
			WHERE <include refid="search_writer" />
		</if>
		<if test="search == 'all'">
			WHERE title LIKE '%' || #{keyword } || '%'
			OR content LIKE '%' || #{keyword } || '%'
			OR <include refid="search_writer" />
		</if>
	</sql>
	
	<sql id="search_writer">
		writer IN (SELECT id FROM member WHERE name LIKE '%' || #{keyword } || '%')
	</sql>
=======

>>>>>>> develop_Park

</mapper>